
//declare a variable

var name;
var age;
var hungry;

//type: "string" (if a number is within " it is considered a string), number, value

name = "John";  //string - 
age = 15;       //number - integers can take positive/negative/decimals (arithmetic operators)
hungry = true;    //boolean - used to represent logic (use boolean operators=and/not/or)
tired = false;    //boolean - used to represent logic (use boolean operators=and/not/or)

console.log('hungry && tired: ' + (hungry && tired));
console.log('hungry || tired: ' + (timeToLeave));

var timeToLeave = hungry || tired;

if (timeToLeave == true) {
    console.log(">>>yes");
} else {
    console.log(">>>no");
}

// single equal is assign / double equal means left equals right

console.log('!hungry && !tired: ' + (hungry && tired));

// alert('hello ' + name);

//string +string = concatenation